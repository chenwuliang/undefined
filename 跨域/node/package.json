{
  "name": "csrf",
  "version": "1.0.0",
  "description": "csrf jsonp",
  "main": "csrf.js",
  "dependencies": {
    "koa": "^2.13.1",
    "koa-logger": "^3.2.1",
    "koa-router": "^10.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "anywhere 8080",
    "server": "nodemon server/csrf.js"
  },
  "keywords": [
    "csrf",
    "jsonp"
  ],
  "author": "Leon",
  "license": "ISC",
  "devDependencies": {
    "anywhere": "^1.5.0",
    "nodemon": "^2.0.7"
  }
}
